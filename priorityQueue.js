// Priority Queue
    // data structure where each element has a priority
    // elements with higher priority are served before elements with lower priority
    
    // pseucode 
    // write a min binary heap
    // each node has a value and priority to build heap
    // enqueue method which accepts a value and priority, makes a new node and put it in order of priority
    // dequeue method removes root element, returns it, and rearrages the heap using priority

class Node{
    constructor(val, priority){
        this.val = val
        this.priority = priority
    }
}

class PriorityQueue {
    
}